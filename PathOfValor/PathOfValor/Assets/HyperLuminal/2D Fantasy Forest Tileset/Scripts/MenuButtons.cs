using UnityEngine;

// Empty placeholder so any existing prefab references stay valid but render no UI.
public class MenuButtons : MonoBehaviour
{
	// Intentionally left blank to remove the original Day/Night buttons.
}
